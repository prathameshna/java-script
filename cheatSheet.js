 // If you delete any element from the array, but the size of the element remains unchanged.
//  MATH:-The JavaScript Math object allows you to perform mathematical tasks on numbers.
// The Math.floor() method rounds a number DOWN to the nearest integer.
// the match() method is used to search a string for a match against a regular expression and returns the matches as an array. 
// ROUND :- round are used to remove last point part of integer.